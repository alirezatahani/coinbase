import styled from "styled-components";

export const HomesStyle = styled.div(({ theme }) => ({
  backgroundColor: theme.palette.background.dark,
  height: "auto",
  textAlign: "center",
  width: 550,
  padding: "2rem",
}));
